node_modules
.next/
dist/

# Environment secrets — NEVER commit real credentials
.env
.env.local
.env.staging
.env.production
.env.*.local
!.env.example
!.env.staging.example

# Local upload storage (real client files — never commit)
apps/api/uploads/

# Terraform local cache/providers — never commit
infra/aws/.terraform/
*.tfstate
*.tfstate.*
